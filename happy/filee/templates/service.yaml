apiVersion: v1
kind: Service
metadata:
  name: {{ include "filee.fullname" . }}
  labels:
    {{- include "filee.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "filee.selectorLabels" . | nindent 4 }}
