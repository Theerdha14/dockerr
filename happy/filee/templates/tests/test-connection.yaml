apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "filee.fullname" . }}-test-connection"
  labels:
    {{- include "filee.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "filee.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
